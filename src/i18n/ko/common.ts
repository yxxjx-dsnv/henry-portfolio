// The shell (navigation, titles, palette, clock) stays in English by the owner's choice;
// only the reading matter switches. Nothing to translate here for now.
const ko: Record<string, string> = {};

export default ko;
