node_modules/
dist/
.vite/
__pycache__/
.DS_Store
*.local

# Obsidian project vault (personal notes; backed up via OneDrive)
Henry's portfolio/

# bkit plugin per-project state (tool state, not site source)
.bkit/

# Obsidian app config (vault opened at repo root)
.obsidian/

# original photo/video sources (web versions live in public/media/)
_media-originals/

# Incheon Robotics working files — pitch decks, presentation scripts, raw CAD
# renders, lab footage, PCB photos, .blend scenes. Company material; it lives in
# ~/Documents/Henry Kim/Work-Incheon Robotics/src-incheon-robotics/ and this rule
# guards against a copy being dropped here. Only the compressed web versions
# under public/media/incheon-robotics/ are ever published.
src-incheon-robotics/

# archived earlier project (kept locally, not part of the live site)
_archive/

# raw APS105 lab dump (build artifacts + PDFs); the served sources are copied
# into src/assets/aps105/ and the toolchain into public/cide/
Labs/

# tool-generated analysis cache
.understand-anything/

# signed personal documents dropped at the repo root (offer letters, agreements —
# they carry a home address, phone number, and signature). Site PDFs live in
# public/media/ and are unaffected by these root-anchored rules.
/*.pdf
/*.docx

# CV / resume sources (LaTeX + originals) — personal documents with phone number
# and work-evidence reports, managed alongside the site but never published
/Resume/
